import { View, Text } from "react-native";
import React from "react";

const EditPost = () => {
  /* 
        responsible for uploading the image
        posting the description, tags, location, vibetag, aspectRatio of the image, filter for the image

        Server will then process the image with the help of sharp library
    */
  return (
    <View>
      <Text>EditPost</Text>
    </View>
  );
};

export default EditPost;
